#include <string>
using namespace std;

#include "Spiff.h"
#include "SpiffReaderCallbackChild.hpp"
using namespace Spiff;

namespace Chromium2
{
	/**
	 * Handles what happens when a new track is available.
	 */
	void SpiffReaderCallbackChild::addTrack(SpiffTrack * track)
	{
		
	}
	
	/**
	 * Handles what happens when all playlist properties are
	 * available.
	 */
	void SpiffReaderCallbackChild::setProps(SpiffProps * props)
	{
	
	}
}
